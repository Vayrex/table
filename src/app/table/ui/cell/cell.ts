import {Layer} from "../layer/layer";

export class Cell {

  private _layer: Layer;

  public setLayer(layer: Layer) {
    this._layer = Layer;
  }

  public getLayer() {
    return this._layer;
  }
}
